2025-10-08:

    未处理好的地方：
    1. execution and Coverage filter 阶段已经保存执行出现bug 或者 crash 信息的功能，目前只是报错，后面考虑是不是可以直接过滤掉跟实际bug无关的信息，不然都需要人工去查，比较耗时间
    2. prompt还可以再优化 (还需要传入参数类型和可能的取值范围, enum可能比较难搞，因为其可能直接是通过int来的，而不是通过enum来的，很不直观，不是人工看到话可能不知道具体对应的是哪个enum
    3，fuzz/test_demo 并行进行



需要检查的：
1. hdf5的hdf5.yaml没检查对不对