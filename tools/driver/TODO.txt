2025-10-08:

    未处理好的地方：
    1. execution and Coverage filter 阶段已经保存执行出现bug 或者 crash 信息的功能，目前只是报错，后面考虑是不是可以直接过滤掉跟实际bug无关的信息，不然都需要人工去查，比较耗时间
    2. prompt还可以再优化 (还需要传入参数类型和可能的取值范围, enum可能比较难搞，因为其可能直接是通过int来的，而不是通过enum来的，很不直观，不是人工看到话可能不知道具体对应的是哪个enum
    3，fuzz/test_demo 并行进行



需要检查的：
1. hdf5的hdf5.yaml没检查对不对
2. exiv2没检查对不对也没有生成harness
3. libical没检查对不对也没有生成harness


Crach Check:

1. cJSON已完成 | fuzz 一小时check完成
2. libmagic已完成 | fuzz 一小时check完成
3. libucl已完成 🪲
4. c-ares已完成 | fuzz 一小时check完成
5. zlib已完成 | fuzz 一小时check完成
6. libpcap已完成 | fuzz 一小时check完成
7. lcms已完成 | fuzz 一小时check完成
8. pcre2已完成 | fuzz 一小时check完成
9. hdf5已完成，但是感觉有问题，是不是需要重新generation一下
10. cgltf已完成
11. StormLib 已完成
12. libtiff harness没检查 | fuzz 一小时check完成



gntlab-server上的运行结果：
------------------------------------------------
配置的库需要注意的点：
1.libzip需要把编译后的zipconf.h复制到lib目录下
2.libtiff需要把编译后的tiffconf.h复制到include目录下


------------------------------------------------

1. zsv已经检查完成, 发现一个bug, 在zsv_set_fixed_offsets函数中, 如果offsets为NULL, 会直接崩溃, 需要修复，已提交issue, 等待修复。https://github.com/liquidaty/zsv/issues/529 这个bug已经确认并修复
2. libplist还没有检查完成，目前检查到plist_dict_get_uint, 发现一个bug, 在plist_dict_copy_int函数中, 如果key为NULL, 会直接崩溃, 需要修复，已提交issue, 等待修复。https://github.com/libimobiledevice/libplist/issues/296
3. liblouis 发现一个memory leak: https://github.com/liblouis/liblouis/issues/1902



------------------------------------------------

candidate libraries:
libarchive
libcoap
libbpf API比较多 
libucl
