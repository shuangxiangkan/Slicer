2025-09-18:
    
    注意：目前generate_harnesses_for_all_apis()代码中只是对有fuzz usage的API进行了测试，还没有对所有API进行测试，后面需要改过来

    编译harness的时候还是报错

    目前感觉寻找source和binary关系是通过step1_successful_harnesses.json的关系来的，但是我感觉太复杂，我觉得就每次只需要保留过滤后的源文件，在执行和覆盖率筛选的时候重新编译就好了，在/tmp/中重新编译执行就好了，这样应该可以简化，所有的编译，执行，fuzz都在tmp/中进行就好了

    编译，执行，覆盖率，每个阶段只需要保留另个文件夹就行了：1. 过滤后的源文件；2.各种log及prompt信息
    而且生成和编译的 源文件/ 和 log文件夹/ 可以只保留一个，